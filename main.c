#include "libft.h"
#include <stdio.h>
#include <string.h>
#include	<stdlib.h>

int main()
{
    char str[] = "--56";
    printf("%d\n", ft_atoi(str));
    printf("%d", atoi(str));
    // printf("%d", 325 / 10);
    return 0;
}
